<div class="wrapper wrapper-content">
  <div id="content">
    <div class="row wrapper border-bottom white-bg page-heading">
      <div class="col-lg-10">
        <h2>List history_timesheet</h2>
        <ol class="breadcrumb">
          <li>
            <a href="">Home</a>
          </li>
          <li>
            <a href="<%= history_timesheets_url %>">List</a>
          </li>
          <li class="active">
            <strong>List</strong>
          </li>
        </ol>
      </div>
    </div>
    <br>
    <div id="content">
      <div class="table-responsive">
        <table class="table table-striped table-bordered table-hover dataTables-example">
          <thead>
          <tr>
            <th>No</th>
            <th>Parent timesheet</th>
            <th>Status</th>
            <th>by</th>
            <th>Info</th>
            <th>Alasan (if any)</th>
            <th>Status Info</th>
            <th>Tertuju Info</th>
            <th></th>
            <th></th>
          </tr>
          </thead>

          <tbody>
          <% @history_timesheets.each_with_index do |history_timesheet, index| %>
            <tr>
              <td><%= index + 1 %></td>
              <td><%= history_timesheet.parent_timesheet_id %></td>
              <td><%= history_timesheet.status %></td>
              <td><%= history_timesheet.by %></td>
              <td><%= history_timesheet.info %></td>
              <td><%= history_timesheet.alasan %></td>
              <td><%= history_timesheet.status_info %></td>
              <td><%= history_timesheet.tertuju_info %></td>
              <td><%= link_to 'Show', history_timesheet %></td>
              <td><%= link_to 'Destroy', history_timesheet, method: :delete, data: {confirm: 'Are you sure?'} %></td>
            </tr>
          <% end %>
          </tbody>
        </table>
      </div>
    </div>
  </div>
</div>

<% content_for :javascript do -%>
  <script type="text/javascript" charset="utf-8">
      $(function () {
          $('.dataTables-example').dataTable({
              dom: "<'myfilter'f><'mylength'l>t<'myfilter'i><'p'p>",
              language: {
                  search: "_INPUT_",
                  searchPlaceholder: "Search..."
              }
          });
      });
  </script>
<% end %>